\begin{frame}
    \frametitle{\problemtitle}
    \begin{block}{Problem}
        % TODO: Remove this comment when you're done writing the solutions.
        Print $4\sqrt n$ with sufficiently many digits.
    \end{block}
    \pause
    \begin{block}{Naive solution}
        Do something stupid.
        $\mathcal O(2^n)$ is too slow!
    \end{block}
    \pause
    \begin{block}{Solution}
        Instead, just compute the answer.
        Run time: $\mathcal O(1)$.
    \end{block}
    \pause
    \solvestats
\end{frame}
