\begin{slide}
	\begin{block}{Problem}
		Given two sets of $n$ DNA strands, determine their repetition score (length of the smallest repeating substring).
		Then, find a perfect matching, minimising the squared difference of the matched pairs' repetition scores.
	\end{block}
  \pause
	\begin{itemize}
		\item Two sub-problems: (i)~Repetition score, (ii)~Matching
	\end{itemize}
\end{slide}

\begin{slide}
	\begin{block}{Solution (i)~Repetition score}
		\begin{itemize}
			\item Simple brute-force approach sufficient.
			\item Better: simplification, based on the following observations:
			\begin{enumerate}
				\item A string consists of the same pattern repeated multiple times if and only if the string is a non-trivial rotation of itself.
				\item If $\mathsf{x}$ and $\mathsf{y}$ are strings of the same length, then $\mathsf{x}$ is a rotation of $\mathsf{y}$ if and only if $\mathsf{x}$ is a substring of $\mathsf{yy}$.
			\end{enumerate}
			\item Pseudocode: $\mathsf{score} = (s + s).\mathsf{find}(s, 1, 2 \cdot \mathsf{len}(s) - 1)$
		\end{itemize}
	\end{block}
  \pause
	\begin{block}{Solution (ii)~Matching}
		\begin{itemize}
			\item Use the Hungarian method (also called Munkres algorithm) for optimal matching (rather complex, but $O(n^3)$).
			\item Better: Sorting and matching yields minimal Euclidean distance in $O(n \log n)$.
		\end{itemize}
	\end{block}
\end{slide}

% https://stackoverflow.com/questions/55823298/how-do-i-check-if-a-string-is-entirely-made-of-the-same-substring
% https://stackoverflow.com/questions/54041397/given-two-arrays-find-the-permutations-that-give-closest-distance-between-two-a
