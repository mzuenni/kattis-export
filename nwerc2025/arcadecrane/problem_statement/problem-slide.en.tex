\begin{frame}
    \frametitle{\problemtitle}

    \begin{columns}
        \begin{column}[T]{.79\textwidth}
            \begin{itemize}
              \item You are given a permutation of the numbers $1$ to $n \leq 1000$ that you need to sort.
              \item In a single step, you can take three consecutive numbers out of the sequence and re-insert them somewhere else.
                % Yes, 'somewhere else' is technically not enforced, but this detail doesn't matter for explaining the problem
              \item Output a sequence of up to $5000$ steps that sorts the sequence.
            \end{itemize}

            \vspace{1em}

            \centering
            \includegraphics[width=0.8\textwidth]{figure.pdf}

            \small
            Illustration of Sample Output 1.
        \end{column}

        %\illustration{0.15}{illustration.jpg}{
        %    Caption of the illustration (optional).
        %    CC BY-SA 4.0 by X on \href{https://example.com/reference-to-image}{Y}
        %}
    \end{columns}
\end{frame}
