REPI(i,3)
	INT(-10^3,10^3,x[i]) SPACE INT(-10^3,10^3,y[i]) NEWLINE
END
FLOAT(0.1,10^6,r) NEWLINE
SET(area = (x[0]*(y[1]-y[2]) + x[1]*(y[2]-y[0]) + x[2]*(y[0]-y[1]))/2.0)
IF (area < 0)
	SET(area = -area)
END
ASSERT(area >= 0.1)
